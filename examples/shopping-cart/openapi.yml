openapi: 3.0.0
info:
  title: Shopping Cart API
  version: 1.0.0
  description: >
    Event-sourced shopping cart powered by Emmett + Express.js.
    Uses OpenAPI validation + security handlers with Firestore EventStore and Realtime DB projections.
servers:
  - url: http://localhost:3000
    description: Local development

paths:
  /clients/{clientId}/shopping-carts/current/product-items:
    post:
      operationId: addProductItem
      x-eov-operation-handler: shoppingCarts
      summary: Add a product item
      security:
        - bearerAuth:
            - cart:write
      parameters:
        - $ref: '#/components/parameters/ClientId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - productId
                - quantity
              properties:
                productId:
                  type: string
                  minLength: 1
                quantity:
                  type: integer
                  minimum: 1
      responses:
        '204':
          description: Product added
        '400':
          $ref: '#/components/responses/ValidationProblem'
        '403':
          description: Shopping cart closed or invalid state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'
        '401':
          description: Missing or invalid token

    delete:
      operationId: removeProductItem
      x-eov-operation-handler: shoppingCarts
      summary: Remove a product item
      security:
        - bearerAuth:
            - cart:write
      parameters:
        - $ref: '#/components/parameters/ClientId'
        - name: productId
          in: query
          required: true
          schema:
            type: string
            minLength: 1
        - name: quantity
          in: query
          required: true
          schema:
            type: integer
            minimum: 1
        - name: unitPrice
          in: query
          required: true
          schema:
            type: number
            minimum: 0
      responses:
        '204':
          description: Product removed
        '400':
          $ref: '#/components/responses/ValidationProblem'
        '403':
          description: Not enough items to remove
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'
        '401':
          description: Missing or invalid token

  /clients/{clientId}/shopping-carts/current/confirm:
    post:
      operationId: confirmShoppingCart
      x-eov-operation-handler: shoppingCarts
      summary: Confirm current cart
      security:
        - bearerAuth:
            - cart:write
      parameters:
        - $ref: '#/components/parameters/ClientId'
      responses:
        '204':
          description: Shopping cart confirmed
        '400':
          $ref: '#/components/responses/ValidationProblem'
        '403':
          description: Shopping cart empty or closed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'
        '401':
          description: Missing or invalid token

  /clients/{clientId}/shopping-carts/current:
    get:
      operationId: getShoppingCart
      x-eov-operation-handler: shoppingCarts
      summary: Get current shopping cart (full details)
      security:
        - bearerAuth:
            - cart:read
      parameters:
        - $ref: '#/components/parameters/ClientId'
      responses:
        '200':
          description: Shopping cart full details
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  clientId:
                    type: string
                  productItems:
                    type: array
                    items:
                      type: object
                      properties:
                        productId:
                          type: string
                        quantity:
                          type: integer
                        unitPrice:
                          type: number
                  totalAmount:
                    type: number
                  productItemsCount:
                    type: integer
                  status:
                    type: string
                    enum: [Opened, Confirmed, Cancelled]
                  openedAt:
                    type: string
                    format: date-time
                  confirmedAt:
                    type: string
                    format: date-time
                  cancelledAt:
                    type: string
                    format: date-time
        '404':
          description: Shopping cart not found
        '401':
          description: Missing or invalid token

    delete:
      operationId: cancelShoppingCart
      x-eov-operation-handler: shoppingCarts
      summary: Cancel the cart
      security:
        - bearerAuth:
            - cart:write
      parameters:
        - $ref: '#/components/parameters/ClientId'
      responses:
        '204':
          description: Shopping cart cancelled
        '400':
          $ref: '#/components/responses/ValidationProblem'
        '403':
          description: Shopping cart not open
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'
        '401':
          description: Missing or invalid token

  /clients/{clientId}/shopping-carts/current/summary:
    get:
      operationId: getShoppingCartSummary
      x-eov-operation-handler: shoppingCarts
      summary: Get shopping cart summary (short info)
      security:
        - bearerAuth:
            - cart:read
      parameters:
        - $ref: '#/components/parameters/ClientId'
      responses:
        '200':
          description: Shopping cart summary
          content:
            application/json:
              schema:
                type: object
                properties:
                  totalAmount:
                    type: number
                  productItemsCount:
                    type: integer
        '404':
          description: Shopping cart not found
        '401':
          description: Missing or invalid token

components:
  parameters:
    ClientId:
      name: clientId
      in: path
      required: true
      schema:
        type: string
        minLength: 1

  schemas:
    ProblemDetails:
      type: object
      properties:
        type:
          type: string
          example: about:blank
        title:
          type: string
          example: Bad Request
        status:
          type: integer
          example: 400
        detail:
          type: string
          example: quantity must be positive
        errors:
          type: array
          items:
            type: object
            properties:
              path:
                type: string
              message:
                type: string
              errorCode:
                type: string

  responses:
    ValidationProblem:
      description: Validation error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ProblemDetails'

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
